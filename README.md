# debruijn_experiments
Experiments for a research paper about the local structure in reasoning traces

Each set of experiments is under a different sub-folder. Use, e.g.,
```python run_experiments.py --devices cuda:0``` in each sub-folder to generate the plots.
